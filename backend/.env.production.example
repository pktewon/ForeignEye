# ============================================
# ForeignEye Google Cloud Run 프로덕션 환경 변수
# ============================================
# 사용 방법: 
# 1. 이 파일을 .env.prod로 복사
# 2. 모든 CHANGE_THIS 값을 실제 값으로 변경
# 3. Google Cloud Run 환경 변수로 설정하거나 Secret Manager 사용

# Flask 설정
SECRET_KEY=CHANGE_THIS_TO_RANDOM_64_CHARACTER_STRING
JWT_SECRET_KEY=CHANGE_THIS_TO_DIFFERENT_RANDOM_64_CHARACTER_STRING
FLASK_ENV=production
FLASK_DEBUG=False

# Cloud Run 포트 설정 (Cloud Run이 자동으로 할당)
# 로컬 테스트: PORT=8080
# Cloud Run: PORT는 자동 설정되므로 여기서 설정 불필요

# ============================================
# Google Cloud SQL 데이터베이스 설정
# ============================================
# Cloud SQL 인스턴스 연결 방법:
# 방법 1: Public IP (비추천 - 보안 취약)
#   DB_HOST=<Cloud SQL Public IP>
#   DB_PORT=3306
#
# 방법 2: Private IP (VPC 연결 필요)
#   DB_HOST=<Cloud SQL Private IP>
#   DB_PORT=3306
#
# 방법 3: Unix Socket (Cloud Run 권장)
#   DB_HOST=/cloudsql/<PROJECT_ID>:<REGION>:<INSTANCE_NAME>
#   예: /cloudsql/foreigneye-project:asia-northeast3:foreigneye-db
#   Cloud Run 설정에서 --add-cloudsql-instances 필요

DB_USER=foreigneye_user
DB_PASSWORD=CHANGE_THIS_TO_YOUR_CLOUD_SQL_PASSWORD
DB_HOST=/cloudsql/YOUR_PROJECT_ID:YOUR_REGION:foreigneye-db
DB_PORT=3306
DB_NAME=foreigneye_db

# API 키
GNEWS_API_KEY=your_gnews_api_key_here
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Redis (캐싱 - 옵션)
REDIS_URL=redis://your-redis-host:6379/0

# ============================================
# CORS 설정 (프론트엔드 도메인)
# ============================================
# Cloud Run URL 또는 커스텀 도메인 사용
# 예: https://foreigneye-frontend-abc123.run.app
# 여러 도메인은 쉼표로 구분
CORS_ORIGINS=https://your-frontend-domain.run.app,https://www.your-custom-domain.com

# 로깅
LOG_LEVEL=INFO
